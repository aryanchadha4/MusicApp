import API_BASE_URL, { AUTH_DISABLED, DEV_USER_EMAIL } from '../../../config';

export const webSessionConfig = {
  baseUrl: API_BASE_URL,
  authDisabled: AUTH_DISABLED,
  devUserEmail: DEV_USER_EMAIL,
};
