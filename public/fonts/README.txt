Place licensed webfont files here for @font-face in src/App.css:

  Graphik-Regular.woff2   → family name "Graphik"
  TiemposText-Regular.woff2 → family name "Tiempos"

If these files are missing, the UI falls back to Inter (body) and Georgia (headings).
